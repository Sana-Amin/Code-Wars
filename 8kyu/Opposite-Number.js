// Very simple, given an integer or a floating-point number, find its opposite.

// Examples:
// 1: -1
// 14: -14
// -34: 34

//My Solution: 
function opposite(number) {
    if (number == number)
      return -number; {
    
      }if(-number == -number){
        return number; 
      }
  }

  function opposite(number) {
    return(-number);
  }

  //Best Solution: 

const opposite = number => -number;

// if the user enters a positive, the negative will be there, if a user enters a negative there will be two negatives changing it into a postive. 